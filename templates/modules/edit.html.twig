{% extends 'base.html.twig' %}

{% block title %}Modifier le droit : {{ module.name }}{% endblock %}

{% block PageTitle %}Modifier le droit : {{ module.name }}{% endblock %}

{% block body %}
<div>
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button type="submit" class="btn btn-primary mt-3">Modifier</button>
    {{ form_end(form) }}
</div>


{% endblock %}